# Data Directory

Large GEO files are downloaded locally into `data/raw_geo/` and are intentionally ignored by Git.

Run:

```bash
python3 scripts/00_download_geo.py --annotation-only
```

for the lightweight annotation-first workflow.

